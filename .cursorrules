# Global Engineering Rules - Agent Skills Pack
#
# This file is the Cursor adapter for the agent-skills engineering pack.
# It injects the always-on rules and provides navigation to the full pack.
#
# Source of truth: core/rules/code.md, core/roles/role-standard.md

# === MANDATORY RULES (Always-On) ===

# META-RULE: Before finalizing any response or executing a command, verify the
# action against core/rules/code.md. If any step violates a rule,
# halt and ask the user for permission.

# Commit & Publish Safety
- Do NOT create a commit unless the user explicitly confirms that specific commit action.
- Do NOT push commits, create tags, or publish releases unless the user explicitly confirms.
- Ensure all code changes pass local linters, unit tests, and build checks before creating a commit.

# Code Standards
- Repo-local rules override these defaults when they are explicitly present.
- Prefer repo-local standards, templates, and workflows when they exist.
- Do not invent repository conventions, paths, branching models, or release rules not present in the active codebase.

# Wording & Metadata
- Do not mention agents, AI workflow, review labels, severity labels, task trackers, or other internal process metadata in commit messages, changelog text, release notes, or other user-visible change notes.
- Do not expose secrets, credentials, tokens, private keys, or sensitive internal values in commits, comments, changelogs, release notes, or other user-visible artifacts.

# Environment Files
- NEVER commit .dev.vars, .env, or other local environment files; verify git status and keep them in .gitignore.

# Code Comments
- Prefer no comment over comments that merely restate the code.
- Keep code comments implementation-focused and useful.
- Keep each code comment within 3 lines unless a longer comment is required for doc comments, file headers, or tooling directives.

# === ROLE SYSTEM ===
#
# When the user assigns a Role, read:
#   1. core/roles/role-standard.md (mandatory base standard)
#   2. core/roles/<role-name>.md (specific role)
#
# Follow SKILL TOOLBOX LOCK: Only use Primary Skills from your role's Skill Toolbox.
# Follow BOUNDARY LOCK: If a task falls outside your role's core responsibilities,
# politely decline and recommend the appropriate role.
#
# Available roles: core/roles/README.md
#
# === A2A 1.0, GRAPH, ANTIGRAVITY (pack 5.0.0) ===
# - Antigravity: read adapters/antigravity/ANTIGRAVITY.md; use core/a2a/.well-known/agent-registry.json for discovery.
# - Full A2A lifecycle: agent-a2a-protocol (task, progress, status, artifact, cancel); JSON-RPC via a2a-jsonrpc-envelope.json.
# - Coordinator: agent-a2a-protocol + agent-graph-orchestration + coordination-plan.json per phase.
# - Obey Policy-as-Code: action-boundaries.yaml + data-classification.yaml before state-changing actions.

# === SKILLS ===
#
# Organized by taxonomy under core/skills/:
#   core/skills/agent/         - context management, memory compaction, tool orchestration, quality gates, handoff
#   core/skills/foundation/    - commit, review, test, navigate, troubleshoot
#   core/skills/backend/       - API, events, integrations, scaffolding
#   core/skills/frontend/      - UI components, pages, API client, testing
#   core/skills/platform/      - deployment, runtime debug, telemetry
#   core/skills/commerce/      - checkout, payment, catalog, fulfillment
#   core/skills/security-data/ - secrets, database, security audit
#   core/skills/documentation/ - technical docs, tech radar
#   core/skills/education/     - learning plans, exercises, grading
#
# Each skill has a SKILL.md. Read it fully before executing.
# Skill index: core/skills/README.md

# === WORKFLOWS ===
#
# When executing a workflow:
#   1. Output a markdown checklist [ ] for ALL steps.
#   2. Process only ONE step at a time, mark [x], explain result.
#   3. Respect the Role: tag on each step.
#
# Available workflows: core/workflows/README.md

## Standard 2026 Alignment

This file is part of the agent-skills engineering pack. The 2026 upgrade
pass added this footer so every prose file in the pack carries a
consistent Standard 2026 pointer.

- **OWASP ASI**: applied as described in `core/roles/role-standard.md`
  (ASI01-ASI10) and the per-skill `## Security Guardrails (OWASP ASI)` sections.
- **Failure Modes**: the rule in this file can be violated by drift, missing
  context, or untracked exceptions. Concrete failure scenarios belong in the
  related skill or workflow's `### Failure Modes` section.
- **Output Contracts**: structured artifacts produced under this file must
  conform to schemas in `core/contracts/schemas/`.
- **Skill Toolbox Lock**: this file's rules are enforced by the role that
  owns the affected action; the runtime gate is
  `core/scripts/hooks/check-policy.py`.
- **Commit / publish gate**: changes that affect user-visible behavior
  follow the META-RULE in `core/rules/code.md` — no commit, no push, no
  publish without explicit user confirmation.

Last updated: 2026-09-02
